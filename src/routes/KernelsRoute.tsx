import PlaceholderRoute from './PlaceholderRoute';

function KernelsRoute() {
  return (
    <PlaceholderRoute
      title="Kernels"
      description="Operator's kernels (physical sites) with status, device count, queue depth, recent jobs, and last heartbeat. Phase 2.1 ports KernelHealthSnapshot rendering."
    />
  );
}

export default KernelsRoute;
