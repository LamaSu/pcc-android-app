import PlaceholderRoute from './PlaceholderRoute';

function EvidenceRoute() {
  return (
    <PlaceholderRoute
      title="Evidence"
      description="Browse evidence bundles by tier, ALCOA+ status, and drift alerts. Phase 2.1 wires the ComplianceFacade DTO + IPFS link rendering."
    />
  );
}

export default EvidenceRoute;
